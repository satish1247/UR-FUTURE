rules_version = '2';
// Browsers never talk to Firestore directly: the Next.js server reads and writes
// with the Admin SDK (which bypasses these rules). So deny everything.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
